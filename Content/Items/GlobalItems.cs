using Microsoft.Xna.Framework;
using System;
using System.Linq;
using Terraria;
using Terraria.Audio;
using Terraria.ID;
using Terraria.Localization;
using Terraria.ModLoader;
using Terraria.Utilities;
using Terraria.GameContent.Bestiary;
using Terraria.GameContent.ItemDropRules;
using Microsoft.Xna.Framework.Graphics;
using Terraria.GameContent;
using Terraria.GameContent.Personalities;
using Terraria.DataStructures;
using System.Collections.Generic;
using ReLogic.Content;
using Terraria.ModLoader.IO;

namespace FortniteItems.Content.Items
{
    public class GlobalItems : GlobalItem
    {
        /*
        public int gunType;
        /// <summary>
        /// Sets the type of gun via an Int. Gun type can be accessed via
        /// the getGunType method.
        /// KEY:
        /// 0 = No Type 
        /// 1 = Assault Rifle
        /// 2 = Shotgun
        /// 3 = Submachine Gun
        /// 4 = Pistol
        /// 5 = Sniper Rifle
        /// 6 = Bow
        /// 7 = Explosive
        /// </summary>
        /// <param name="type"></param>
        public void SetGunType(int type)
        {
            this.gunType = type;
        }

        /// <summary>
        /// Returns the integer stored in gunType.
        /// gunType can be used to tell other items the type of gun 
        /// the player is holding. The gun type can be set via the
        /// setGunType method.
        /// KEY:
        /// 0 = No Type
        /// 1 = Assault Rifle
        /// 2 = Shotgun
        /// 3 = Submachine Gun
        /// 4 = Pistol
        /// 5 = Sniper Rifle
        /// 6 = Bow
        /// 7 = Explosive
        /// </summary>
        /// <returns></returns>
        public int GetGunType()
        {
            return gunType;
        }
        */
    }
}
